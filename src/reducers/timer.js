const initialState = {
  timeLeft: 10 * 60 * 60 * 1000,
  timeSpent: 0,
  isActive: false
}

const timer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default timer
